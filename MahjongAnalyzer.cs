namespace MahjongMaster {
    public static class MahjongAnalyzer {
        public static string SuggestDiscard(string[] hand) {
            // Placeholder: return last tile as discard
            return hand.Length > 0 ? hand[^1] : "";
        }
    }
}
